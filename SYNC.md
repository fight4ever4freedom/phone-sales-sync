# 外网/多地点同步说明

这个版本支持云端同步：把整个文件夹放到一台云服务器或 VPS 上，运行 `server.js`，手机和电脑访问同一个公网地址即可同步。

## 本地测试

双击 `start-sync.bat`，然后电脑打开：

```text
http://localhost:8787
```

手机同 Wi-Fi 可打开终端里显示的 `LAN address`。

## 外网使用

1. 准备一台云服务器或可部署 Node.js 的平台。
2. 上传本文件夹全部文件。
3. 设置环境变量：

```text
PORT=8787
SYNC_TOKEN=你自己的同步密码
```

4. 运行：

```text
node server.js
```

5. 用公网域名或公网 IP 打开页面。第一次访问会要求输入同步密码。

数据保存在服务器同目录的 `data.json`。请定期备份这个文件。
